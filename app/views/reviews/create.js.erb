<% if @review.errors.any? %>
  $("#review-errors").html("<%= j(render partial:'review_form_errors') %>");
<% else %>
  $('#review-list').html("<%= j(render @product.reviews) %>");
  $('#review_comment').val('');
  $("#review-errors").html('');
<% end %> 